#! /usr/bin/env python

class Expr:

	def __init__(self, op, left, right):
		self.op 	= op
		self.left 	= left
		self.right	= right
	
	def eval(self):
		if left

	def checkExpr(self, n):
		try:
			x = int(n)
			return x
		except ValueError:
			return None
